---
title: "Number of Connected Components in an Undirected Graph"
difficulty: "Medium"
topic: "graph"
leetcodeUrl: "https://leetcode.com/problems/number-of-connected-components-in-an-undirected-graph"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/graph/NumberOfConnectedComponentsInAnUndirectedGraph.java"
patterns: ["union-find","dfs"]
tags: []
timeComplexity: "O(V+E)"
spaceComplexity: "O(V)"
isPremium: false
hasProblemStatement: true
javaApis: []
similarProblems: []
enrichmentNeeded: true
---

You have a graph of n nodes. You are given an integer n and an array edges where edges[i] = [ai, bi] indicates that
there is an undirected edge between nodes ai and bi in the graph.

Return the number of connected components in the graph.


**Example 1:**

**Input: n = 5, edges = [[0,1],[1,2],[3,4]]**

**Output: 2**


**Example 2:**

**Input: n = 5, edges = [[0,1],[1,2],[2,3],[3,4]]**

**Output: 1**

**Patterns:** `union-find`

## Java Solution

```java
package graph;

/**
 * You have a graph of n nodes. You are given an integer n and an array edges where edges[i] = [ai, bi] indicates that
 * there is an undirected edge between nodes ai and bi in the graph.
 *
 * Return the number of connected components in the graph.
 *
 * Example 1:
 * Input: n = 5, edges = [[0,1],[1,2],[3,4]]
 * Output: 2
 *
 * Example 2:
 * Input: n = 5, edges = [[0,1],[1,2],[2,3],[3,4]]
 * Output: 1
 */
public class NumberOfConnectedComponentsInAnUndirectedGraph {
    public int countComponents(int n, int[][] edges) {
        int[] parent = new int[n];
        int[] rank = new int[n];
        for (int i = 0; i < n; i++) {
            parent[i] = i;
            rank[i] = 1;
        }

        int components = n;

        for (int[] edge : edges) {
            if (union(edge[0], edge[1], parent, rank)) {
                components--;
            }
        }

        return components;
    }

    private boolean union(int x, int y, int[] parent, int[] rank) {
        int rootX = find(x, parent);
        int rootY = find(y, parent);
        if (rootX == rootY) {
            return false;
        }

        if (rank[rootX] > rank[rootY]) {
            parent[rootY] = rootX;
        } else if (rank[rootX] < rank[rootY]) {
            parent[rootX] = rootY;
        } else {
            parent[rootY] = rootX;
            rank[rootX]++;
        }
        return true;
    }

    private int find(int x, int[] parent) {
        if (parent[x] != x) {
            parent[x] = find(parent[x], parent);
        }
        return parent[x];
    }
}
```

## Explanation

### Approach

**Number of Connected Components in an Undirected Graph** involves repeatedly merging groups and querying whether two elements belong to the same group — perfect for Union-Find. The data structure maintains a forest of trees where each tree represents a component; union merges two trees and find identifies a root.

With path compression (flattening the tree on every find) and union by rank (always attaching the smaller tree under the larger), both operations run in near-constant amortised time O(α(n)), where α is the extremely slow-growing inverse Ackermann function.

### Code Walkthrough

The solution is implemented in `countComponents(int n, int[][] edges)`:

1. Initialise parent[i] = i and rank[i] = 0 for every node.
2. find(x): recursively find the root, applying path compression along the way.
3. union(x, y): find both roots; if different, attach the lower-rank tree under the higher-rank one.
4. After processing all edges/queries, count distinct roots or answer connectivity queries.

> **Note:** This solution also uses the **DFS to explore all paths or connected components recursively** technique.

### Complexity Analysis

- **Time Complexity: `O(V+E)`** — each vertex and each edge is processed at most once in the graph traversal.
- **Space Complexity: `O(V)`** — auxiliary space proportional to the input is required.
