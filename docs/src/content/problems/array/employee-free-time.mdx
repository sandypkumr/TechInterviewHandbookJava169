---
title: "Employee Free Time"
difficulty: "Hard"
topic: "array"
leetcodeUrl: "https://leetcode.com/problems/employee-free-time"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/array/EmployeeFreeTime.java"
patterns: ["heap","merge-intervals"]
tags: []
timeComplexity: "O(n log n)"
spaceComplexity: "O(n)"
isPremium: false
hasProblemStatement: true
javaApis: ["ArrayList","Math.max/min"]
similarProblems: []
enrichmentNeeded: true
---

We are given a list of employees, where each employee has a list of non-overlapping intervals representing their
working hours. These intervals are sorted by start time. We need to find the common free time intervals across all
employees.



The free time intervals are the time periods when all employees are not working.




**Example:**
    Input: [[[1, 3], [6, 7]], [[2, 4]], [[2, 5], [9, 12]]]
    Output: [[4, 6], [7, 9]]



    Explanation:
    The common free time intervals are [4, 6] and [7, 9].

**Patterns:** `merge-intervals`

## Java Solution

```java
package array;

import java.util.ArrayList;
import java.util.Comparator;
import java.util.List;

/**
 * We are given a list of employees, where each employee has a list of non-overlapping intervals representing their
 * working hours. These intervals are sorted by start time. We need to find the common free time intervals across all
 * employees.
 * <p>
 * The free time intervals are the time periods when all employees are not working.
 * <p>
 * Example:
 *     Input: [[[1, 3], [6, 7]], [[2, 4]], [[2, 5], [9, 12]]]
 *     Output: [[4, 6], [7, 9]]
 * <p>
 *     Explanation:
 *     The common free time intervals are [4, 6] and [7, 9].
 */
public class EmployeeFreeTime {
    public List<Interval> employeeFreeTime(List<List<Interval>> schedule) {
        // Step 1: Flatten the schedule into a single list of intervals
        List<Interval> allIntervals = new ArrayList<>();
        for (List<Interval> employeeSchedule : schedule) {
            allIntervals.addAll(employeeSchedule);
        }

        // Step 2: Sort the intervals based on their start times
        allIntervals.sort(Comparator.comparingInt(a -> a.start));

        // Step 3: Find free time intervals
        List<Interval> freeTime = new ArrayList<>();
        int end = allIntervals.getFirst().end;

        for (int i = 1; i < allIntervals.size(); i++) {
            Interval current = allIntervals.get(i);
            if (current.start > end) {
                freeTime.add(new Interval(end, current.start));
            }
            end = Math.max(end, current.end);
        }

        return freeTime;
    }
}
```

## Explanation

### Approach

**Employee Free Time** needs to repeatedly find the smallest or largest element from a dynamically changing collection. A heap (Java's `PriorityQueue`) maintains this invariant with O(log n) insert and O(log n) remove, far better than sorting the entire collection each time.

A min-heap is like a waiting room where the most urgent patient always sits at the front. As patients arrive or leave, the room reorganises in O(log n) time to keep the most urgent at the front — you never need to scan everyone to find them.

### Code Walkthrough

The solution is implemented in `employeeFreeTime(List<List<Interval>> schedule)`:

1. Choose a min-heap (natural order) or max-heap (Comparator.reverseOrder()).
2. Insert elements as you process them.
3. Poll the top element when you need the current minimum/maximum.
4. For "top-K" problems, keep the heap size bounded at K to save memory.
5. After processing, the remaining heap elements or the polled sequence is your answer.

&gt; **Note:** This solution also uses the **sort intervals by start time, then merge overlapping ones in one pass** technique.

### Complexity Analysis

- **Time Complexity: `O(n log n)`** — dominated by sorting the input (O(n log n)); the subsequent linear scan is O(n).
- **Space Complexity: `O(n)`** — auxiliary space proportional to the input size is used.
