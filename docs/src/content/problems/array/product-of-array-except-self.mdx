---
title: "Product of Array Except Self"
difficulty: "Medium"
topic: "array"
leetcodeUrl: "https://leetcode.com/problems/product-of-array-except-self"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/array/ProductOfArrayExceptSelf.java"
patterns: ["prefix-sum"]
tags: ["classic"]
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
isPremium: false
hasProblemStatement: false
javaApis: []
similarProblems: []
enrichmentNeeded: true
---

Given an integer array `nums`, return _an array_ `answer` _such that_ `answer[i]` _is equal to the product of all the elements of_ `nums` _except_ `nums[i]`.

The product of any prefix or suffix of `nums` is **guaranteed** to fit in a **32-bit** integer.

You must write an algorithm that runs in `O(n)` time and without using the division operation.

**Example 1:**

```
Input: nums = [1,2,3,4]
Output: [24,12,8,6]
```
**Example 2:**

```
Input: nums = [-1,1,0,-3,3]
Output: [0,0,9,0,0]
```

**Constraints:**

- `2 <= nums.length <= 10^5`

- `-30 <= nums[i] <= 30`

- The input is generated such that `answer[i]` is **guaranteed** to fit in a **32-bit** integer.

**Follow up:** Can you solve the problem in `O(1)` extra space complexity? (The output array **does not** count as extra space for space complexity analysis.)

## Java Solution

```java
package array;

public class ProductOfArrayExceptSelf {
    public int[] productExceptSelf(int[] nums) {
        int n = nums.length;
        int[] result = new int[n];
        int[] leftProducts = new int[n];
        int[] rightProducts = new int[n];

        leftProducts[0] = 1;
        for (int i = 1; i < n; i++) {
            leftProducts[i] = leftProducts[i - 1] * nums[i - 1];
        }

        rightProducts[n - 1] = 1;
        for (int i = n - 2; i >= 0; i--) {
            rightProducts[i] = rightProducts[i + 1] * nums[i + 1];
        }

        for (int i = 0; i < n; i++) {
            result[i] = leftProducts[i] * rightProducts[i];
        }

        return result;
    }
}
```

## Explanation

### Approach

**Product of Array Except Self** requires repeated range-sum queries. Computing each sum from scratch is O(n) per query; a prefix sum array precomputes cumulative totals so any range sum [l, r] reduces to prefixSum[r] - prefixSum[l - 1] — a single O(1) subtraction.

Imagine a running tally of rainfall: instead of adding up each day's rain for every query, you keep a running total. The rain from day l to day r is simply totalUpToR - totalUpToL-1.

### Code Walkthrough

The solution is implemented in `productExceptSelf(int[] nums)`:

1. Allocate prefix[n + 1] initialised to 0.
2. Fill prefix[i] = prefix[i - 1] + nums[i - 1] for i from 1 to n.
3. Answer a range query [l, r] (0-indexed) as prefix[r + 1] - prefix[l].
4. Use a HashMap from prefix-sum → first index to detect subarrays with a target sum.

### Complexity Analysis

- **Time Complexity: `O(n)`** — the array is traversed exactly once; all HashMap/HashSet operations are O(1) amortised.
- **Space Complexity: `O(1)`** — only a fixed number of extra variables are used; no data structures that scale with input.
