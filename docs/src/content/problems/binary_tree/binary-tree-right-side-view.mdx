---
title: "Binary Tree Right Side View"
difficulty: "Medium"
topic: "binary_tree"
leetcodeUrl: "https://leetcode.com/problems/binary-tree-right-side-view"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/binary_tree/BinaryTreeRightSideView.java"
patterns: ["bfs","dfs"]
tags: []
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
isPremium: false
hasProblemStatement: false
javaApis: ["LinkedList","ArrayList"]
similarProblems: []
enrichmentNeeded: true
---

Given the `root` of a binary tree, imagine yourself standing on the **right side** of it, return _the values of the nodes you can see ordered from top to bottom_.

**Example 1:**

**Input:** root = [1,2,3,null,5,null,4]

**Output:** [1,3,4]

**Explanation:**

**Example 2:**

**Input:** root = [1,2,3,4,null,null,null,5]

**Output:** [1,3,4,5]

**Explanation:**

**Example 3:**

**Input:** root = [1,null,3]

**Output:** [1,3]

**Example 4:**

**Input:** root = []

**Output:** []

**Constraints:**

- The number of nodes in the tree is in the range `[0, 100]`.

- `-100 <= Node.val <= 100`

**Patterns:** `bfs`

## Java Solution

```java
package binary_tree;

import java.util.ArrayList;
import java.util.LinkedList;
import java.util.List;
import java.util.Queue;

public class BinaryTreeRightSideView {
    public List<Integer> rightSideView(TreeNode root) {
        List<Integer> result = new ArrayList<>();
        if (root == null) {
            return result;
        }
        Queue<TreeNode> queue = new LinkedList<>();
        queue.offer(root);
        while (!queue.isEmpty()) {
            int size = queue.size();
            for (int i = 0; i < size; i++) {
                TreeNode node = queue.poll();
                if (i == size - 1) {
                    result.add(node.val);
                }
                if (node.left != null) {
                    queue.offer(node.left);
                }
                if (node.right != null) {
                    queue.offer(node.right);
                }
            }
        }
        return result;
    }
}
```

## Explanation

### Approach

**Binary Tree Right Side View** requires visiting nodes in order of their distance from the source — exactly what Breadth-First Search (BFS) guarantees. By processing nodes level by level via a queue (FIFO), BFS finds the shortest path in an unweighted graph and naturally handles all reachability questions.

Think of it as ripples spreading outward from a stone dropped in water. All nodes at distance 1 are visited before any node at distance 2, and so on. This property makes BFS the right tool whenever "minimum steps" or "level order" is part of the problem.

### Code Walkthrough

The solution is implemented in `rightSideView(TreeNode root)`:

1. Add the source node(s) to a queue and mark them visited.
2. While the queue is non-empty, poll the front node.
3. Process the current node (update answer, record level, etc.).
4. Enqueue all unvisited neighbours and mark them visited immediately to avoid re-processing.
5. The first time you reach the target, you have the shortest path.

> **Note:** This solution also uses the **DFS to explore all paths or connected components recursively** technique.

### Complexity Analysis

- **Time Complexity: `O(n)`** — the input is processed in a single linear pass.
- **Space Complexity: `O(n)`** — the BFS queue can hold up to O(n) nodes at the widest level.
