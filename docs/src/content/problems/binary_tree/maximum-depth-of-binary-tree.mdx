---
title: "Maximum Depth of Binary Tree"
difficulty: "Easy"
topic: "binary_tree"
leetcodeUrl: "https://leetcode.com/problems/maximum-depth-of-binary-tree"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/binary_tree/MaximumDepthOfBinaryTree.java"
patterns: ["dfs","bfs"]
tags: []
timeComplexity: "O(n)"
spaceComplexity: "O(h)"
isPremium: false
hasProblemStatement: false
javaApis: ["Math.max/min"]
similarProblems: []
enrichmentNeeded: true
---

Given the `root` of a binary tree, return _its maximum depth_.

A binary tree's **maximum depth** is the number of nodes along the longest path from the root node down to the farthest leaf node.

**Example 1:**

```
Input: root = [3,9,20,null,null,15,7]
Output: 3
```

**Example 2:**

```
Input: root = [1,null,2]
Output: 2
```

**Constraints:**

- The number of nodes in the tree is in the range `[0, 10^4]`.

- `-100 <= Node.val <= 100`

## Java Solution

```java
package binary_tree;

public class MaximumDepthOfBinaryTree {
    public int maxDepth(TreeNode root) {
        if (root == null) {
            return 0;
        }
        return Math.max(maxDepth(root.left), maxDepth(root.right)) + 1;
    }
}
```

## Explanation

### Approach

**Maximum Depth of Binary Tree** needs to explore all possible paths or the full extent of a connected region — the classic use-case for Depth-First Search (DFS). DFS dives as deep as possible along one branch before backtracking, making it ideal for connectivity, cycle detection, and exhaustive enumeration.

Imagine navigating a maze by always turning left until you hit a dead end, then backtracking to the last junction and trying the next option. DFS systematically explores every possibility without re-visiting the same cell.

### Code Walkthrough

The solution is implemented in `maxDepth(TreeNode root)`:

1. Choose a starting node and mark it as visited.
2. Recursively (or with an explicit stack) visit each unvisited neighbour.
3. Perform the required computation during entry or exit of each node.
4. Backtrack naturally when recursion returns — the call stack handles the state.
5. Repeat for all unvisited nodes to cover disconnected components if needed.

> **Note:** This solution also uses the **BFS to explore nodes level by level** technique.

### Complexity Analysis

- **Time Complexity: `O(n)`** — the input is processed in a single linear pass.
- **Space Complexity: `O(h)`** — auxiliary space proportional to the input is required.
