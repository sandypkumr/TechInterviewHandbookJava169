---
title: "Jump Game"
difficulty: "Medium"
topic: "dynamic_programming"
leetcodeUrl: "https://leetcode.com/problems/jump-game"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/dynamic_programming/JumpGame.java"
patterns: ["greedy","dynamic-programming"]
tags: []
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
isPremium: false
hasProblemStatement: false
javaApis: ["Math.max/min"]
similarProblems: []
enrichmentNeeded: true
---

You are given an integer array `nums`. You are initially positioned at the array's **first index**, and each element in the array represents your maximum jump length at that position.

Return `true`_ if you can reach the last index, or _`false`_ otherwise_.

**Example 1:**

```
Input: nums = [2,3,1,1,4]
Output: true
Explanation: Jump 1 step from index 0 to 1, then 3 steps to the last index.
```

**Example 2:**

```
Input: nums = [3,2,1,0,4]
Output: false
Explanation: You will always arrive at index 3 no matter what. Its maximum jump length is 0, which makes it impossible to reach the last index.
```

**Constraints:**

- `1 <= nums.length <= 10^4`

- `0 <= nums[i] <= 10^5`

## Java Solution

```java
package dynamic_programming;

public class JumpGame {
    public boolean canJump(int[] nums) {
        int maxReach = 0;

        for (int i = 0; i < nums.length; i++) {
            if (i > maxReach) {
                return false;
            }
            maxReach = Math.max(maxReach, i + nums[i]);
        }
        return true;
    }
}
```

## Explanation

### Approach

**Jump Game** has a greedy property: at every decision point, the locally optimal choice leads to a globally optimal solution. Unlike DP, which considers all sub-problems, a greedy algorithm commits to one choice and never reconsiders.

The challenge with greedy is proving correctness (an exchange argument usually works: show that swapping any two elements in a non-greedy solution to match the greedy order never makes things worse). Once you accept the greedy choice is safe, the algorithm is typically simple and fast.

### Code Walkthrough

The solution is implemented in `canJump(int[] nums)`:

1. Identify the greedy criterion (sort by end time, sort by cost, always pick the max/min, etc.).
2. Sort or arrange the input according to this criterion.
3. Iterate and make the greedy decision at each step without revisiting previous choices.
4. Update state (current end, running sum, remaining capacity) after each decision.
5. Return the accumulated result.

&gt; **Note:** This solution also uses the **dynamic programming — break into overlapping subproblems and memoize** technique.

### Complexity Analysis

- **Time Complexity: `O(n)`** — the input is processed in a single linear pass.
- **Space Complexity: `O(1)`** — only a fixed number of extra variables are used; no data structures that scale with input.
