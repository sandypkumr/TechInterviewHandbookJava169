---
title: "Find K Closest Elements"
difficulty: "Medium"
topic: "heap"
leetcodeUrl: "https://leetcode.com/problems/find-k-closest-elements"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/heap/FindKClosestElements.java"
patterns: ["binary-search","two-pointers"]
tags: []
timeComplexity: "O(log(n-k)+k)"
spaceComplexity: "O(1)"
isPremium: false
hasProblemStatement: false
javaApis: []
similarProblems: []
enrichmentNeeded: true
---

> 📝 **No problem description found in source.** Visit [LeetCode](https://leetcode.com/problems/find-k-closest-elements) for the problem statement.

**Patterns:** `sliding-window`

## Java Solution

```java
package heap;

import java.util.Arrays;
import java.util.List;

public class FindKClosestElements {
    public List<Integer> findClosestElements(int[] arr, int k, int x) {
        int left = 0;
        int right = arr.length - k;

        while (left < right) {
            int mid = left + (right - left) / 2;
            if (x - arr[mid] > arr[mid + k] - x) {
                left = mid + 1;
            } else {
                right = mid;
            }
        }

        return Arrays.stream(arr, left, left + k).boxed().toList();
    }
}
```
