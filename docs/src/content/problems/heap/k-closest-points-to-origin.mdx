---
title: "K Closest Points to Origin"
difficulty: "Medium"
topic: "heap"
leetcodeUrl: "https://leetcode.com/problems/k-closest-points-to-origin"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/heap/KClosestPointsToOrigin.java"
patterns: ["heap"]
tags: []
timeComplexity: "O(n log k)"
spaceComplexity: "O(k)"
isPremium: false
hasProblemStatement: false
javaApis: ["PriorityQueue"]
similarProblems: []
enrichmentNeeded: true
---

Given an array of `points` where `points[i] = [xi, yi]` represents a point on the **X-Y** plane and an integer `k`, return the `k` closest points to the origin `(0, 0)`.

The distance between two points on the **X-Y** plane is the Euclidean distance (i.e., `&radic;(x1 - x2)^2 + (y1 - y2)^2`).

You may return the answer in **any order**. The answer is **guaranteed** to be **unique** (except for the order that it is in).

**Example 1:**

```
Input: points = [[1,3],[-2,2]], k = 1
Output: [[-2,2]]
Explanation:
The distance between (1, 3) and the origin is sqrt(10).
The distance between (-2, 2) and the origin is sqrt(8).
Since sqrt(8) < sqrt(10), (-2, 2) is closer to the origin.
We only want the closest k = 1 points from the origin, so the answer is just [[-2,2]].
```

**Example 2:**

```
Input: points = [[3,3],[5,-1],[-2,4]], k = 2
Output: [[3,3],[-2,4]]
Explanation: The answer [[-2,4],[3,3]] would also be accepted.
```

**Constraints:**

- `1 <= k <= points.length <= 10^4`

- `-10^4 <= xi, yi <= 10^4`

**Patterns:** `heap`

## Java Solution

```java
package heap;

import java.util.PriorityQueue;

public class KClosestPointsToOrigin {
    public int[][] kClosest(int[][] points, int k) {
        int[][] result = new int[k][2];
        PriorityQueue<int[]> maxHeap = new PriorityQueue<>(
                (a, b) -> Integer.compare(b[0] * b[0] + b[1] * b[1], a[0] * a[0] + a[1] * a[1])
        );

        for (int[] point : points) {
            maxHeap.offer(point);
            if (maxHeap.size() > k) {
                maxHeap.poll();
            }
        }

        for (int i = 0; i < k; i++) {
            result[i] = maxHeap.poll();
        }

        return result;
    }
}
```

## Explanation

### Approach

**K Closest Points to Origin** needs to repeatedly find the smallest or largest element from a dynamically changing collection. A heap (Java's `PriorityQueue`) maintains this invariant with O(log n) insert and O(log n) remove, far better than sorting the entire collection each time.

A min-heap is like a waiting room where the most urgent patient always sits at the front. As patients arrive or leave, the room reorganises in O(log n) time to keep the most urgent at the front — you never need to scan everyone to find them.

### Code Walkthrough

The solution is implemented in `kClosest(int[][] points, int k)`:

1. Choose a min-heap (natural order) or max-heap (Comparator.reverseOrder()).
2. Insert elements as you process them.
3. Poll the top element when you need the current minimum/maximum.
4. For "top-K" problems, keep the heap size bounded at K to save memory.
5. After processing, the remaining heap elements or the polled sequence is your answer.

### Complexity Analysis

- **Time Complexity: `O(n log k)`** — see the analysis for full details.
- **Space Complexity: `O(k)`** — auxiliary space proportional to the input is required.
