---
title: "Merge k Sorted Lists"
difficulty: "Hard"
topic: "heap"
leetcodeUrl: "https://leetcode.com/problems/merge-k-sorted-lists"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/heap/MergeKSortedLists.java"
patterns: ["heap","divide-and-conquer"]
tags: ["classic"]
timeComplexity: "O(n log k)"
spaceComplexity: "O(k)"
isPremium: false
hasProblemStatement: false
javaApis: []
similarProblems: []
enrichmentNeeded: true
---

You are given an array of `k` linked-lists `lists`, each linked-list is sorted in ascending order.

_Merge all the linked-lists into one sorted linked-list and return it._

**Example 1:**

```
Input: lists = [[1,4,5],[1,3,4],[2,6]]
Output: [1,1,2,3,4,4,5,6]
Explanation: The linked-lists are:
[
  1->4->5,
  1->3->4,
  2->6
]
merging them into one sorted linked list:
1->1->2->3->4->4->5->6
```

**Example 2:**

```
Input: lists = []
Output: []
```

**Example 3:**

```
Input: lists = [[]]
Output: []
```

**Constraints:**

- `k == lists.length`

- `0 <= k <= 10^4`

- `0 <= lists[i].length <= 500`

- `-10^4 <= lists[i][j] <= 10^4`

- `lists[i]` is sorted in **ascending order**.

- The sum of `lists[i].length` will not exceed `10^4`.

**Patterns:** `sliding-window`, `merge-intervals`

## Java Solution

```java
package heap;

public class MergeKSortedLists {
    public ListNode mergeKLists(ListNode[] lists) {
        if (lists == null || lists.length == 0) {
            return null;
        }
        return mergeKLists(lists, 0, lists.length - 1);
    }

    private ListNode mergeKLists(ListNode[] lists, int left, int right) {
        if (left == right) {
            return lists[left];
        }
        int mid = left + (right - left) / 2;
        ListNode l1 = mergeKLists(lists, left, mid);
        ListNode l2 = mergeKLists(lists, mid + 1, right);
        return mergeTwoLists(l1, l2);
    }

    private ListNode mergeTwoLists(ListNode l1, ListNode l2) {
        ListNode dummy = new ListNode(0);
        ListNode current = dummy;

        while (l1 != null && l2 != null) {
            if (l1.val < l2.val) {
                current.next = l1;
                l1 = l1.next;
            } else {
                current.next = l2;
                l2 = l2.next;
            }
            current = current.next;
        }

        if (l1 != null) {
            current.next = l1;
        } else {
            current.next = l2;
        }

        return dummy.next;
    }
}
```

## Explanation

### Approach

**Merge k Sorted Lists** needs to repeatedly find the smallest or largest element from a dynamically changing collection. A heap (Java's `PriorityQueue`) maintains this invariant with O(log n) insert and O(log n) remove, far better than sorting the entire collection each time.

A min-heap is like a waiting room where the most urgent patient always sits at the front. As patients arrive or leave, the room reorganises in O(log n) time to keep the most urgent at the front — you never need to scan everyone to find them.

### Code Walkthrough

The solution is implemented in `mergeKLists(ListNode[] lists)`:

1. Choose a min-heap (natural order) or max-heap (Comparator.reverseOrder()).
2. Insert elements as you process them.
3. Poll the top element when you need the current minimum/maximum.
4. For "top-K" problems, keep the heap size bounded at K to save memory.
5. After processing, the remaining heap elements or the polled sequence is your answer.

> **Note:** This solution also uses the **divide and conquer — split, solve recursively, combine** technique.

### Complexity Analysis

- **Time Complexity: `O(n log k)`** — see the analysis for full details.
- **Space Complexity: `O(k)`** — auxiliary space proportional to the input is required.
