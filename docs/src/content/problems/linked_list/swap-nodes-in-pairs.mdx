---
title: "Swap Nodes in Pairs"
difficulty: "Medium"
topic: "linked_list"
leetcodeUrl: "https://leetcode.com/problems/swap-nodes-in-pairs"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/linked_list/SwapNodesInPairs.java"
patterns: []
tags: []
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
isPremium: false
hasProblemStatement: false
javaApis: []
similarProblems: []
enrichmentNeeded: true
---

Given a linked list, swap every two adjacent nodes and return its head. You must solve the problem without modifying the values in the list's nodes (i.e., only nodes themselves may be changed.)

**Example 1:**

**Input:** head = [1,2,3,4]

**Output:** [2,1,4,3]

**Explanation:**

**Example 2:**

**Input:** head = []

**Output:** []

**Example 3:**

**Input:** head = [1]

**Output:** [1]

**Example 4:**

**Input:** head = [1,2,3]

**Output:** [2,1,3]

**Constraints:**

- The number of nodes in the list is in the range `[0, 100]`.

- `0 <= Node.val <= 100`

## Java Solution

```java
package linked_list;

public class SwapNodesInPairs {
    public ListNode swapPairs(ListNode head) {
        if (head == null || head.next == null) {
            return head;
        }

        ListNode dummy = new ListNode(0);
        dummy.next = head;
        ListNode prev = dummy;

        while (prev.next != null && prev.next.next != null) {
            ListNode first = prev.next;
            ListNode second = first.next;

            // Swap the nodes
            first.next = second.next;
            second.next = first;
            prev.next = second;

            // Move prev pointer two nodes ahead
            prev = first;
        }

        return dummy.next;
    }
}
```

## Explanation

### Approach

**Swap Nodes in Pairs** is a linked list problem. The solution iterates over the input using a iterative loop, applying the appropriate data structure (built-in arrays) to achieve the target complexities of `O(n)` time and `O(1)` space.

### Code Walkthrough

The solution is implemented in `swapPairs(ListNode head)`:

1. **Initialise** the necessary data structures.
2. **Iterate** over the input using a iterative loop.
3. **Process** each element: apply the core logic and update state.
4. **Return** the accumulated result after the loop.

### Complexity Analysis

- **Time Complexity: `O(n)`** — the input is processed in a single linear pass.
- **Space Complexity: `O(1)`** — only a fixed number of extra variables are used; no data structures that scale with input.
