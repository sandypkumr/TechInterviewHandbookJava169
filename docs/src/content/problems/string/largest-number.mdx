---
title: "Largest Number"
difficulty: "Medium"
topic: "string"
leetcodeUrl: "https://leetcode.com/problems/largest-number"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/string/LargestNumber.java"
patterns: []
tags: []
timeComplexity: ""
spaceComplexity: ""
isPremium: false
hasProblemStatement: false
javaApis: ["Arrays.sort","StringBuilder"]
similarProblems: []
enrichmentNeeded: true
---

Given a list of non-negative integers `nums`, arrange them such that they form the largest number and return it.

Since the result may be very large, so you need to return a string instead of an integer.

**Example 1:**

```
Input: nums = [10,2]
Output: "210"
```

**Example 2:**

```
Input: nums = [3,30,34,5,9]
Output: "9534330"
```

**Constraints:**

- `1 <= nums.length <= 100`

- `0 <= nums[i] <= 10^9`

## Java Solution

```java
package string;

import java.util.Arrays;

public class LargestNumber {
    public String largestNumber(int[] nums) {
        String[] strNums = new String[nums.length];
        for (int i = 0; i < nums.length; i++) {
            strNums[i] = String.valueOf(nums[i]);
        }

        Arrays.sort(strNums, (a, b) -> (b + a).compareTo(a + b));

        if (strNums[0].equals("0")) {
            return "0";
        }

        StringBuilder result = new StringBuilder();
        for (String str : strNums) {
            result.append(str);
        }

        return result.toString();
    }
}
```

## Explanation

### Approach

**Largest Number** is a string problem. The solution iterates over the input using a indexed for-loop, applying the appropriate data structure (Arrays.sort, StringBuilder) to achieve the target complexities of `O(?)` time and `O(?)` space.

### Code Walkthrough

The solution is implemented in `largestNumber(int[] nums)`:

1. **Initialise** the necessary data structures (`i`).
2. **Iterate** over the input using a indexed for-loop.
3. **Process** each element: apply the core logic and update state.
4. **Return** the accumulated result after the loop.

### Complexity Analysis

- **Time Complexity: `?`** — see analysis above.
- **Space Complexity: `?`** — see analysis above.
