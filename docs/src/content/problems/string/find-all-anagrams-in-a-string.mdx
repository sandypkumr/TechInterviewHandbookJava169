---
title: "Find All Anagrams in a String"
difficulty: "Medium"
topic: "string"
leetcodeUrl: "https://leetcode.com/problems/find-all-anagrams-in-a-string"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/string/FindAllAnagramsInAString.java"
patterns: ["sliding-window","hash-map"]
tags: []
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
isPremium: false
hasProblemStatement: false
javaApis: ["ArrayList"]
similarProblems: []
enrichmentNeeded: true
---

> 📝 **No problem description found in source.** Visit [LeetCode](https://leetcode.com/problems/find-all-anagrams-in-a-string) for the problem statement.

## Java Solution

```java
package string;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class FindAllAnagramsInAString {
    public List<Integer> findAnagrams(String s, String p) {
        List<Integer> result = new ArrayList<>();
        int sLength = s.length();
        int pLength = p.length();

        if (sLength < pLength) {
            return result;
        }

        int[] pCount = new int[26];
        int[] sCount = new int[26];

        for (int i = 0; i < pLength; i++) {
            pCount[p.charAt(i) - 'a']++;
            sCount[s.charAt(i) - 'a']++;
        }

        if (Arrays.equals(pCount, sCount)) {
            result.add(0);
        }

        for (int i = pLength; i < sLength; i++) {
            sCount[s.charAt(i) - 'a']++;
            sCount[s.charAt(i - pLength) - 'a']--;

            if (Arrays.equals(pCount, sCount)) {
                result.add(i - pLength + 1);
            }
        }

        return result;
    }
}
```
