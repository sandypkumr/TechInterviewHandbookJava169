---
title: "Longest Palindromic Substring"
difficulty: "Medium"
topic: "string"
leetcodeUrl: "https://leetcode.com/problems/longest-palindromic-substring"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/string/LongestPalindromicSubstring.java"
patterns: ["two-pointers"]
tags: []
timeComplexity: "O(n²)"
spaceComplexity: "O(1)"
isPremium: false
hasProblemStatement: false
javaApis: ["Math.max/min"]
similarProblems: []
enrichmentNeeded: true
---

Given a string `s`, return _the longest_ _palindromic_ _substring_ in `s`.

**Example 1:**

```
Input: s = "babad"
Output: "bab"
Explanation: "aba" is also a valid answer.
```

**Example 2:**

```
Input: s = "cbbd"
Output: "bb"
```

**Constraints:**

- `1 <= s.length <= 1000`

- `s` consist of only digits and English letters.

**Patterns:** `sliding-window`, `merge-intervals`

## Java Solution

```java
package string;

public class LongestPalindromicSubstring {
    public String longestPalindrome(String s) {
        int n = s.length();
        if (n < 2) {
            return s;
        }
        int start = 0, end = 0;
        for (int i = 0; i < n; i++) {
            int len1 = expandAroundCenter(s, i, i); // Odd length palindrome
            int len2 = expandAroundCenter(s, i, i + 1); // Even length palindrome
            int len = Math.max(len1, len2);
            if (len > end - start) {
                start = i - (len - 1) / 2;
                end = i + len / 2;
            }
        }
        return s.substring(start, end + 1);
    }

    private int expandAroundCenter(String s, int left, int right) {
        while (left >= 0 && right < s.length() && s.charAt(left) == s.charAt(right)) {
            left--;
            right++;
        }
        return right - left - 1;
    }
}
```

## Explanation

### Approach

**Longest Palindromic Substring** exploits sorted order (or a structural property) to let two pointers converge from opposite ends of the array, eliminating the need for a nested loop. When the current pair doesn't satisfy the condition, we know exactly which pointer to advance — so each element is visited at most once, giving O(n) time.

Imagine squeezing a tube of toothpaste from both ends simultaneously — instead of trying every possible pair (both ends → O(n²)), you make an informed decision at each step about which end to squeeze next.

### Code Walkthrough

The solution is implemented in `longestPalindrome(String s)`:

1. Sort the array if it isn't already (or confirm the existing order guarantees correctness).
2. Place one pointer at the start (left = 0) and one at the end (right = n - 1).
3. Compute the result for the current pair.
4. If the result is too small, advance left to increase it; if too large, move right backward.
5. Stop when the pointers meet or cross.

### Complexity Analysis

- **Time Complexity: `O(n²)`** — requires examining all pairs of elements, leading to a quadratic number of comparisons.
- **Space Complexity: `O(1)`** — only a fixed number of extra variables are used; no data structures that scale with input.
