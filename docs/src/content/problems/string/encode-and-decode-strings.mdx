---
title: "Encode and Decode Strings"
difficulty: "Medium"
topic: "string"
leetcodeUrl: "https://leetcode.com/problems/encode-and-decode-strings"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/string/EncodeAndDecodeStrings.java"
patterns: []
tags: []
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
isPremium: false
hasProblemStatement: true
javaApis: ["ArrayList","StringBuilder","Integer.parseInt"]
similarProblems: []
enrichmentNeeded: true
---

Design an algorithm to encode a list of strings to a string.
The encoded string is then sent over the network and is
decoded back to the original list of strings.

Please implement encode and decode


**Example:**
    Input: ["Hello", "World", "this is a test"]
    codec = Solution()
    encoded_string = codec.encode(["Hello", "World", "this is a test"])
    print(encoded_string)
    Output: "5#Hello5#World14#this is a test"
    decoded_string = codec.decode(encoded_string)
    print(decoded_string)
    Output: ["Hello", "World", "this is a test"]

## Java Solution

```java
package string;

import java.util.ArrayList;
import java.util.List;

/**
 * Design an algorithm to encode a list of strings to a string.
 * The encoded string is then sent over the network and is
 * decoded back to the original list of strings.
 *
 * Please implement encode and decode
 *
 * Example:
 *     Input: ["Hello", "World", "this is a test"]
 *     codec = Solution()
 *     encoded_string = codec.encode(["Hello", "World", "this is a test"])
 *     print(encoded_string)
 *     Output: "5#Hello5#World14#this is a test"
 *     decoded_string = codec.decode(encoded_string)
 *     print(decoded_string)
 *     Output: ["Hello", "World", "this is a test"]
 */
public class EncodeAndDecodeStrings {
    // Encodes a list of strings to a single string.
    public String encode(String[] strs) {
        StringBuilder sb = new StringBuilder();
        for (String str : strs) {
            sb.append(str.length()).append('#').append(str);
        }
        return sb.toString();
    }

    // Decodes a single string to a list of strings.
    public List<String> decode(String s) {
        int i = 0;
        List<String> result = new ArrayList<>();
        while (i < s.length()) {
            int j = s.indexOf('#', i);
            int length = Integer.parseInt(s.substring(i, j));
            result.add(s.substring(j + 1, j + 1 + length));
            i = j + 1 + length;
        }
        return result;
    }
}
```

## Explanation

### Approach

**Encode and Decode Strings** is a string problem. The solution iterates over the input using a while loop with two pointers, applying the appropriate data structure (ArrayList, StringBuilder, Integer.parseInt) to achieve the target complexities of `O(n)` time and `O(n)` space.

### Code Walkthrough

The solution is implemented in `encode(String[] strs)`:

1. **Initialise** the necessary data structures (`sb`, `i`, `j`).
2. **Iterate** over the input using a while loop with two pointers.
3. **Process** each element: apply the core logic and update state.
4. **Return** the accumulated result after the loop.

### Complexity Analysis

- **Time Complexity: `O(n)`** — the input is processed in a single linear pass.
- **Space Complexity: `O(n)`** — auxiliary space proportional to the input size is used.
