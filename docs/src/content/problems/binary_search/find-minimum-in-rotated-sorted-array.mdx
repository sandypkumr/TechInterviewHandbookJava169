---
title: "Find Minimum in Rotated Sorted Array"
difficulty: "Medium"
topic: "binary_search"
leetcodeUrl: "https://leetcode.com/problems/find-minimum-in-rotated-sorted-array"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/binary_search/FindMinimumInRotatedSortedArray.java"
patterns: ["binary-search"]
tags: []
timeComplexity: "O(log n)"
spaceComplexity: "O(1)"
isPremium: false
hasProblemStatement: false
javaApis: []
similarProblems: []
enrichmentNeeded: true
---

Suppose an array of length `n` sorted in ascending order is **rotated** between `1` and `n` times. For example, the array `nums = [0,1,2,4,5,6,7]` might become:

- `[4,5,6,7,0,1,2]` if it was rotated `4` times.

- `[0,1,2,4,5,6,7]` if it was rotated `7` times.

Notice that **rotating** an array `[a[0], a[1], a[2], ..., a[n-1]]` 1 time results in the array `[a[n-1], a[0], a[1], a[2], ..., a[n-2]]`.

Given the sorted rotated array `nums` of **unique** elements, return _the minimum element of this array_.

You must write an algorithm that runs in `O(log n) time`.

**Example 1:**

```
Input: nums = [3,4,5,1,2]
Output: 1
Explanation: The original array was [1,2,3,4,5] rotated 3 times.
```

**Example 2:**

```
Input: nums = [4,5,6,7,0,1,2]
Output: 0
Explanation: The original array was [0,1,2,4,5,6,7] and it was rotated 4 times.
```

**Example 3:**

```
Input: nums = [11,13,15,17]
Output: 11
Explanation: The original array was [11,13,15,17] and it was rotated 4 times.
```

**Constraints:**

- `n == nums.length`

- `1 <= n <= 5000`

- `-5000 <= nums[i] <= 5000`

- All the integers of `nums` are **unique**.

- `nums` is sorted and rotated between `1` and `n` times.

**Patterns:** `sliding-window`

## Java Solution

```java
package binary_search;

public class FindMinimumInRotatedSortedArray {
    public int findMin(int[] nums) {
        int left = 0;
        int right = nums.length - 1;

        while (left < right) {
            int mid = left + (right - left) / 2;

            // Decide which half to continue searching
            if (nums[mid] < nums[right]) {
                right = mid;
            } else {
                left = mid + 1;
            }
        }

        return nums[left]; // The minimum element
    }
}
```

## Explanation

### Approach

**Find Minimum in Rotated Sorted Array** has a monotonic property: we can always determine whether the answer lies in the left or right half of the remaining search space. Binary search exploits this by halving the space at every step, achieving O(log n) instead of O(n) linear scan.

Picture looking up a word in a dictionary: you open to the middle, decide whether your word is in the left or right half, and discard the other half entirely. After at most log₂(n) steps you've found your word — or confirmed it isn't there.

### Code Walkthrough

The solution is implemented in `findMin(int[] nums)`:

1. Define left and right boundaries covering all possible answers.
2. Compute mid = left + (right - left) / 2 (avoids integer overflow).
3. Evaluate the predicate at mid to decide which half to discard.
4. Narrow the range: move left = mid + 1 or right = mid - 1.
5. Repeat until left > right; the answer is typically left (or right + 1).

### Complexity Analysis

- **Time Complexity: `O(log n)`** — the search space is halved at each step (binary search).
- **Space Complexity: `O(1)`** — only a fixed number of extra variables are used; no data structures that scale with input.
