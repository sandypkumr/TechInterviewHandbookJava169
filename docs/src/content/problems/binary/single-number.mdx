---
title: "Single Number"
difficulty: "Easy"
topic: "binary"
leetcodeUrl: "https://leetcode.com/problems/single-number"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/binary/SingleNumber.java"
patterns: []
tags: ["xor-trick"]
timeComplexity: "O(n)"
spaceComplexity: "O(1)"
isPremium: false
hasProblemStatement: false
javaApis: []
similarProblems: []
enrichmentNeeded: true
---

Given a **non-empty** array of integers `nums`, every element appears _twice_ except for one. Find that single one.

You must implement a solution with a linear runtime complexity and use only constant extra space.

**Example 1:**

**Input:** nums = [2,2,1]

**Output:** 1

**Example 2:**

**Input:** nums = [4,1,2,1,2]

**Output:** 4

**Example 3:**

**Input:** nums = [1]

**Output:** 1

**Constraints:**

- `1 <= nums.length <= 3 * 10^4`

- `-3 * 10^4 <= nums[i] <= 3 * 10^4`

- Each element in the array appears twice except for one element which appears only once.

## Java Solution

```java
package binary;

public class SingleNumber {
    public int singleNumber(int[] nums) {
        int result = 0;
        for (int num : nums) {
            result ^= num;
        }
        return result;
    }
}
```

## Explanation

### Approach

**Single Number** is a binary problem. The solution iterates over the input using a enhanced for-each loop, applying the appropriate data structure (built-in arrays) to achieve the target complexities of `O(n)` time and `O(1)` space.

### Code Walkthrough

The solution is implemented in `singleNumber(int[] nums)`:

1. **Initialise** the necessary data structures.
2. **Iterate** over the input using a enhanced for-each loop.
3. **Process** each element: apply the core logic and update state.
4. **Return** the accumulated result after the loop.

### Complexity Analysis

- **Time Complexity: `O(n)`** — the input is processed in a single linear pass.
- **Space Complexity: `O(1)`** — only a fixed number of extra variables are used; no data structures that scale with input.
