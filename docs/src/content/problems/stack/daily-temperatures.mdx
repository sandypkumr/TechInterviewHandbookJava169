---
title: "Daily Temperatures"
difficulty: "Medium"
topic: "stack"
leetcodeUrl: "https://leetcode.com/problems/daily-temperatures"
githubUrl: "https://github.com/sandypkumr/TechInterviewHandbookJava169/blob/master/src/stack/DailyTemperatures.java"
patterns: ["monotonic-stack"]
tags: []
timeComplexity: "O(n)"
spaceComplexity: "O(n)"
isPremium: false
hasProblemStatement: false
javaApis: ["Stack"]
similarProblems: []
enrichmentNeeded: true
---

Given an array of integers `temperatures` represents the daily temperatures, return _an array_ `answer` _such that_ `answer[i]` _is the number of days you have to wait after the_ `i^th` _day to get a warmer temperature_. If there is no future day for which this is possible, keep `answer[i] == 0` instead.

**Example 1:**

```
Input: temperatures = [73,74,75,71,69,72,76,73]
Output: [1,1,4,2,1,1,0,0]
```
**Example 2:**

```
Input: temperatures = [30,40,50,60]
Output: [1,1,1,0]
```
**Example 3:**

```
Input: temperatures = [30,60,90]
Output: [1,1,0]
```

**Constraints:**

- `1 <= temperatures.length <= 10^5`

- `30 <= temperatures[i] <= 100`

## Java Solution

```java
package stack;

import java.util.Stack;

public class DailyTemperatures {
    public int[] dailyTemperatures(int[] temperatures) {
        int n = temperatures.length;
        int[] result = new int[n];
        Stack<Integer> stack = new Stack<>();

        for (int i = 0; i < n; i++) {
            while (!stack.isEmpty() && temperatures[i] > temperatures[stack.peek()]) {
                int index = stack.pop();
                result[index] = i - index;
            }
            stack.push(i);
        }

        return result;
    }
}
```

## Explanation

### Approach

**Daily Temperatures** asks about the relationship between each element and the next element that is greater (or smaller) than it. A brute-force nested loop is O(n²); a monotonic stack solves it in O(n) by maintaining a stack that is always in increasing (or decreasing) order.

As you scan left to right, every time a new element would break the stack's monotonic property, you pop elements off — each pop reveals that the current element is the "next greater" for the popped element. Each element is pushed and popped at most once, giving O(n) total time.

### Code Walkthrough

The solution is implemented in `dailyTemperatures(int[] temperatures)`:

1. Initialise an empty stack (storing indices, not values, for flexibility).
2. For each element, while the stack is non-empty and the condition is met, pop and record the answer.
3. Push the current index onto the stack.
4. After the full scan, remaining stack elements have no next-greater element (answer = -1 or 0).

### Complexity Analysis

- **Time Complexity: `O(n)`** — the input is processed in a single linear pass.
- **Space Complexity: `O(n)`** — auxiliary space proportional to the input size is used.
